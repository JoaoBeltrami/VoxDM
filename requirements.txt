# VoxDM — dependencias pinadas (reproducibilidade).
# Versoes refletem o conjunto known-good instalado e validado (879 testes verdes).
# Atualizar uma dep: bump aqui, rodar a suite, e so commitar se passar.
# Stack ML (faster-whisper/torch/ctranslate2) e fragil — nao floatar essas.

# ─── Core ─────────────────────────────────────────────────────────────────────
pydantic-settings==2.13.1
structlog==25.5.0
tenacity==9.1.4
python-dotenv==1.2.2
httpx==0.28.1
numpy==2.4.4

# ─── Fase 1 — Ingestão ───────────────────────────────────────────────────────
PyMuPDF==1.27.2.2
google-genai==1.69.0
groq==1.1.2
sentence-transformers==5.4.0
qdrant-client==1.17.1
neo4j==6.1.0
aiosqlite==0.22.1

# ─── Fase 2 — Voz ──────────────────────────────────────────────────────────────
RealtimeSTT==0.1.7
faster-whisper==1.2.1
av==17.0.1             # PyAV — decoder WebM/Opus para faster-whisper (necessario no Windows)
edge-tts==7.2.8
kokoro==0.9.4
pygame==2.6.1

# ─── Fase 3 — LLM e Memória ─────────────────────────────────────────────────────
langsmith==0.7.37
streamlit==1.56.0
streamlit-autorefresh==1.0.1

# ─── Fase 4 — API ───────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn[standard]==0.46.0
websockets==15.0.1
python-multipart==0.0.27

# ─── Segurança ──────────────────────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
bcrypt==5.0.0          # pin explicito — passlib 1.7.4 + bcrypt 5.x tem warning de versao, mas funciona
slowapi==0.1.9         # rate limiting in-memory pra /transcribe, /session/start, /turn
pyjwt[crypto]==2.12.1

# ─── Dev ────────────────────────────────────────────────────────────────────────
pytest==9.0.2
pytest-asyncio==1.3.0

# ─── Nota: torch instalado separadamente com CUDA ────────────────────────────────
# uv pip install torch --index-url https://download.pytorch.org/whl/cu124
